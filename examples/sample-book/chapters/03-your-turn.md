# Your Turn

Clone the workflow for your next project:

1. Run `ebook-factory init` to scaffold a book.
2. Replace the sample chapters with your manuscript.
3. Run `ebook-factory build` whenever you want a fresh EPUB.

For multiple titles, put each book in its own folder and run `ebook-factory batch books/`.

Happy publishing.
